name: Build OnePlus Pad 2 NetHunter Kernel (Snapdragon 8 Gen 3)

on:
  workflow_dispatch:

jobs:
  build:
    name: Compile GKI 6.1 Kernel (Android 16)
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Android Kernel with NetHunter and KernelSU-Next
        uses: dabao1955/kernel_build_action@main
        with:
          # Android 16 GKI 6.1 Kernel (OnePlus Pad 2 / Snapdragon 8 Gen 3)
          kernel-url: https://android.googlesource.com/kernel/manifest
          kernel-branch: android16-6.1
          config: gki_defconfig
          arch: arm64

          # Toolchain Setup - BOTH REQUIRED
          aosp-clang: true
          aosp-gcc: true
          android-version: 13

          # KernelSU-Next Integration
          ksu: true
          ksu-other: true
          ksu-url: https://github.com/bmax121/KernelSU
          ksu-version: next

          # NetHunter Support
          nethunter: true
          nethunter-patch: true

          # Merge custom kernel configs for NetHunter + device optimization
          merge-configs: |
            [
              "configs/nethunter-base.txt",
              "configs/nethunter-network.txt",
              "configs/snapdragon8-gen3-optimize.txt",
              "configs/security-hardening.txt"
            ]

          # Package as flashable AnyKernel3 zip
          anykernel3: true
          release: true
          access-token: ${{ secrets.GITHUB_TOKEN }}
